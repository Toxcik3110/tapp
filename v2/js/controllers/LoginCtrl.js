app.controller('LoginCtrl', ['$scope', '$rootScope', function($scope, $rootScope) {
/**
    Controller for logining.
    Creating user object with email and pass fields
    If email and pass not null, then auto-login (if already stored in local storage)
**/

}]);
